// Knip exception: index.html loads this classic script before the Vite entry;
// the container entrypoint overwrites it at runtime, so no source import exists.
window.__EXCALIDASH_RUNTIME_CONFIG__ = { errorTrackerDsn: "" };
